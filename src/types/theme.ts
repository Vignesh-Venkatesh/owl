export interface OwlTheme {
  name: string;
  colors: {
    bg: string;
    surface: string;
    surfaceAlt: string;
    border: string;
    text: string;
    textDim: string;
    accent: string;
    muted: string;
    danger: string;
    good: string;
  };
}
